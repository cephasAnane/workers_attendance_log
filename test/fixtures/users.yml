<% require 'bcrypt' %>

one:
  full_name: Cephas             # Changed from 'name'
  email: cephas@example.com
  # Changed from 'password_digest' to 'encrypted_password'
  encrypted_password: <%= ::BCrypt::Password.create('password', cost: 4) %>
  department: one

two:
  full_name: Another User       # Changed from 'name'
  email: other@example.com
  # Changed from 'password_digest' to 'encrypted_password'
  encrypted_password: <%= ::BCrypt::Password.create('password', cost: 4) %>
  department: two